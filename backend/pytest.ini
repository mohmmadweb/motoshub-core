[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
python_files = tests.py test_*.py *_tests.py
addopts = -q
# The WebSocket consumer tests are async; markers declare their own loop.
asyncio_mode = auto
